<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <link href="https://stackpath.bootstrapcdn.com/bootswatch/4.5.0/lux/bootstrap.min.css" rel="stylesheet" integrity="sha384-smnSwzHqW1zKbeuSMsAM/fMQpkk7HY11LuHiwT8snL/W2QBoZtVCT4H5x1CEcJCs" crossorigin="anonymous">
  <link rel="stylesheet" href="/styles/styles.css" media="screen" title="no title" charset="utf-8">
  <title></title>
</head>
<body>
  <div id="home">
  <div class="container">
    <div class="row">
      <div class="col-8 offset-2">
        <h3 style="margin-top:50px">Create New Traveler</h3>
        <form class="clearfix mb-4" action="POST">
          <div class="form-group">
            <label for="example-text">Traveler:</label>
            <input type="text" id="traveler-name" class="form-control" aria-describedby="example-text"
              placeholder="Traveler">
          </div>
          <div class="form-group">
            <label for="example-description">Hometown</label>
            <input type="text" class="form-control" id="hometown" aria-describedby="example-description"
              placeholder="Hometown"></textarea>
          </div>
          <button id="submit-user" class="btn btn-primary float-right">Submit</button>
        </form>
        <form class="clearfix mb-4" action="POST">
          <div class="form-group">
            <label for="category">Select Traveler:</label>
            <select class="selectpicker custom-select" id="traveler">
              <option></option>
              {{#each user}}
              <option value="{{this.id}}">{{this.userName}}</option>
              {{/each}}

            </select>
          
          <button id="submit" type="button" class="btn btn-primary float-right">Submit</button>
        </form>
       
        <h3 style="margin-top:50px">Trips taken:</h3>
        <div id="example-list" class="container">
          {{#each destination}}
          <div data-id="{{this.id}}" class="row">
            <div class="col-4">
              <p>Departure City: {{this.departureCity}}</p>
              <p>Arrival City: {{this.arrivalCity}}</p>
              <p>Trip Distance: {{this.tripDistance}}</p>
              <p>{{this.User.userName}}</p>
            </div>
            <div class="col-4">
              <p>Stuff Did:</p>
              <ul>
                {{#each Activities}}
                <li>{{this.activityName}}</li>
                {{/each}}
              </ul>
            </div>
            <div class="col-4">
              <a href="/trip/{{this.id}}"><button type="button" class="btn btn-primary float-right">View Trip</button></a>
            </div>
          </div>
          {{/each}} 

        </div>
      </div>
    </div>
  </div>

  <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
  {{!-- <script src="/../node_modules/geolib/lib/index.js" type="application/javascript"></script> --}}
  <script src="/js/index.js"></script>
</body>
</html>

